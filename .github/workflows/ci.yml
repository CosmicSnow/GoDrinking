name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npx tsc --noEmit

  rust:
    # windows-latest on purpose: the incident lives here (OpenH264 SPS
    # profile, Media Foundation fallback) and the live encoder tests are
    # #[cfg(target_os = "windows")].
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      # The Sala loopback E2E tests spawn a real `node rendezvous/server.mjs`,
      # which needs its `ws` dependency installed. Without this, the spawn
      # dies on import and the tests time out waiting for the port.
      - run: npm ci --prefix rendezvous
      - run: cargo test --manifest-path src-tauri/Cargo.toml
        env:
          # Same as release.yml: Opus's bundled CMakeLists still declares
          # cmake_minimum_required < 3.5, which stock CMake now rejects.
          CMAKE_POLICY_VERSION_MINIMUM: 3.5
